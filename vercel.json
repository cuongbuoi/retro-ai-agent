{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".output",
  "github": {
    "enabled": true,
    "silent": false
  },
  "cleanUrls": true,
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }],
  "regions": ["sin1"]
}
